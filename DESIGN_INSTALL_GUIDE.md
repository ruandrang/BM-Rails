# 🎨 농구 클럽 매니저 - 새 디자인 설치 가이드

## 📦 준비된 파일

1. `tailwind.config.js` - Tailwind 설정 파일
2. `application.html.erb` - 새 레이아웃 파일
3. `clubs_index.html.erb` - 새 대시보드 페이지

---

## 🚀 설치 순서

### 1단계: Tailwind CSS 설치

```bash
# 프로젝트 루트에서 실행
bin/rails tailwindcss:install
```

만약 에러가 나면:

```bash
bundle add tailwindcss-rails
bin/rails tailwindcss:install
```

### 2단계: DaisyUI 설치

```bash
npm install -D daisyui@latest
```

npm이 없다면 yarn 사용:

```bash
yarn add -D daisyui
```

### 3단계: 기존 파일 백업

```bash
# 백업 폴더 생성
mkdir -p backup/$(date +%Y%m%d)

# 기존 파일 백업
cp app/views/layouts/application.html.erb backup/$(date +%Y%m%d)/
cp app/views/clubs/index.html.erb backup/$(date +%Y%m%d)/
```

### 4단계: 새 파일 적용

다운로드 받은 파일들을 다음 위치에 복사:

```bash
# tailwind.config.js는 프로젝트 루트에
cp 다운로드경로/tailwind.config.js ./

# application.html.erb는 layouts 폴더에
cp 다운로드경로/application.html.erb app/views/layouts/

# clubs_index.html.erb는 clubs 폴더에
cp 다운로드경로/clubs_index.html.erb app/views/clubs/index.html.erb
```

### 5단계: 기존 CSS 파일 정리 (선택사항)

기존 순수 CSS 스타일을 제거하거나 주석 처리:

```bash
# app/assets/stylesheets/application.css 파일을 열어서
# 기존 커스텀 CSS를 주석 처리하거나 삭제
```

### 6단계: 서버 재시작

```bash
# 서버 중지 (Ctrl+C)
# 그 다음 다시 시작
bin/rails server
```

---

## ✅ 확인 사항

서버 실행 후 http://localhost:3000 접속해서 확인:

- [ ] 사이드바가 왼쪽에 표시되는가?
- [ ] 농구공 오렌지 색상이 적용되었는가?
- [ ] 클럽 카드가 3열 그리드로 표시되는가?
- [ ] 모바일에서도 잘 보이는가? (브라우저 개발자 도구로 확인)
- [ ] 통계 카드가 상단에 표시되는가?

---

## 🐛 문제 해결

### "tailwindcss: command not found" 에러

```bash
bundle add tailwindcss-rails
bin/rails tailwindcss:install
```

### DaisyUI 적용 안 됨

`tailwind.config.js` 파일의 plugins 부분 확인:

```javascript
plugins: [
  require('daisyui'),
],
```

### 스타일이 전혀 적용 안 됨

Tailwind CSS 빌드 프로세스 실행:

```bash
bin/rails tailwindcss:build
```

또는 개발 모드로 자동 빌드:

```bash
bin/rails tailwindcss:watch
```

### 서버 재시작해도 변경사항 안 보임

브라우저 캐시 삭제:
- Chrome: Cmd+Shift+R (Mac) / Ctrl+Shift+R (Windows)
- 또는 시크릿 모드로 접속

---

## 📱 모바일 테스트

브라우저 개발자 도구에서 모바일 뷰 테스트:

1. F12 키 (개발자 도구 열기)
2. 상단의 모바일 아이콘 클릭
3. iPhone 또는 Galaxy 선택
4. 사이드바가 햄버거 메뉴로 변경되는지 확인

---

## 🎨 다음 단계

대시보드가 잘 적용되었다면:

1. 멤버 관리 페이지 디자인 (다음에 제공)
2. 경기 기록 페이지 디자인
3. 통계 대시보드 디자인
4. 라이브 점수판 디자인

---

## 💬 문의

- 에러 발생 시: 전체 에러 메시지 복사해서 Claude에게 질문
- 디자인 수정 원하면: 어떤 부분을 바꾸고 싶은지 알려주세요
- 다른 페이지 디자인 필요하면: 페이지 이름만 알려주시면 만들어드립니다!

**화이팅! 🏀**
