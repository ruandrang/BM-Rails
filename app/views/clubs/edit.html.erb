<% content_for :title, t('clubs.edit.page_title', name: @club.name) %>

<div class="max-w-5xl mx-auto">
  <div class="mb-6">
    <h1 class="page-title text-base-content mb-2"><%= t('clubs.edit.title') %></h1>
    <p class="text-base-content/60"><%= t('clubs.edit.subtitle') %></p>
  </div>

  <%= render "form" %>
</div>
