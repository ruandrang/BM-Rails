<% content_for :title, "클럽 편집" %>

<div class="page-header">
  <div>
    <h1>클럽 편집</h1>
    <p class="muted">클럽 정보를 업데이트하세요.</p>
  </div>
  <div class="actions">
    <%= link_to "뒤로가기", club_path(@club), class: "button ghost" %>
  </div>
</div>

<div class="card">
  <%= render "form" %>
</div>
