<% content_for :title, t('scoreboards.index.page_title') %>

<div class="max-w-7xl mx-auto">
  <!-- 뒤로가기 및 헤더 -->
  <div class="mb-4">
    <%= link_to clubs_path, class: "btn btn-ghost btn-sm gap-2" do %>
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
      </svg>
      <%= t('common.back') %>
    <% end %>
  </div>

  <!-- 페이지 헤더 -->
  <div class="card bg-gradient-to-r from-primary to-secondary text-primary-content mb-8">
    <div class="card-body">
      <div class="flex items-center gap-3">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-5a2 2 0 012-2h2a2 2 0 012 2v5m-6 0h6m4 0a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2h14z" />
        </svg>
        <div>
          <h1 class="text-4xl font-black italic"><%= t('scoreboards.index.title') %></h1>
          <p class="text-primary-content/80 mt-1"><%= t('scoreboards.index.subtitle') %></p>
        </div>
      </div>
    </div>
  </div>

  <!-- 클럽별 경기 목록 -->
  <div class="space-y-8">
    <% if @clubs.empty? %>
      <div class="alert alert-info shadow-lg">
        <div>
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current flex-shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span><%= t('scoreboards.index.no_clubs') %></span>
        </div>
        <%= link_to t('scoreboards.index.create_club'), new_club_path, class: "btn btn-primary btn-sm" %>
      </div>
    <% end %>

    <% @clubs.each do |club| %>
      <div class="card bg-base-100 shadow-xl">
        <div class="card-body">
          <h2 class="card-title text-2xl mb-4">
            <span class="text-3xl"><%= club_icon(club.icon) %></span>
            <%= club.name %>
          </h2>

          <% if club.matches.any? %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
              <% club.matches.order(played_on: :desc).each do |match| %>
                <div class="card bg-base-200 shadow hover:shadow-lg transition-shadow">
                  <div class="card-body p-5">
                    <!-- 경기 날짜 -->
                    <div class="flex items-center justify-between mb-3">
                      <div class="flex items-center gap-2">
                        <div class="badge badge-primary badge-lg font-bold">
                          <%= match.played_on.strftime("%m/%d") %>
                        </div>
                        <div class="badge badge-outline"><%= t('scoreboards.index.quarters', count: match.regular_quarters_count) %></div>
                      </div>
                      <div class="badge badge-outline"><%= t('scoreboards.index.teams_match', count: match.teams_count) %></div>
                    </div>

                    <!-- 경기 정보 -->
                    <div class="text-center mb-4">
                      <div class="text-lg font-bold text-base-content/70">
                        <%= l(match.played_on, format: :long) %>
                      </div>
                      <% if match.note.present? %>
                        <p class="text-sm text-base-content/60 mt-1 truncate"><%= match.note %></p>
                      <% end %>
                    </div>

                    <!-- 액션 버튼 -->
                    <div class="flex flex-col gap-2">
                      <%= link_to scoreboard_club_match_path(club, match), class: "btn btn-primary btn-block gap-2" do %>
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4" />
                        </svg>
                        <%= t('scoreboards.index.open_control') %>
                      <% end %>
                      <%= link_to scoreboard_display_club_match_path(club, match), class: "btn btn-outline btn-block gap-2", target: "_blank" do %>
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                        </svg>
                        <%= t('scoreboards.index.open_display') %>
                      <% end %>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
          <% else %>
            <div class="text-center py-8">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-base-content/30 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-5a2 2 0 012-2h2a2 2 0 012 2v5m-6 0h6m4 0a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2h14z" />
              </svg>
              <p class="text-base-content/70 mb-4"><%= t('scoreboards.index.no_matches') %></p>
              <%= link_to new_club_match_path(club), class: "btn btn-primary gap-2" do %>
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                </svg>
                <%= t('scoreboards.index.create_match') %>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
