<% content_for :title, "세팅" %>

<div class="max-w-5xl mx-auto">
  <div class="mb-6">
    <h1 class="text-4xl font-bold text-base-content mb-2">세팅</h1>
    <p class="text-base-content/60">점수판 기본 경기 시간을 설정할 수 있습니다.</p>
  </div>

  <div class="card bg-base-100 shadow-xl border border-base-300">
    <div class="card-body p-6 md:p-8">
      <h2 class="card-title text-2xl mb-2">경기 시간 설정</h2>
      <p class="text-base-content/60 mb-6">여기서 설정한 시간이 점수판의 기본 경기 시간으로 사용됩니다.</p>

      <%= form_with model: @user, url: setting_path, method: :patch, local: true, class: "space-y-6" do |form| %>
        <div class="form-control w-full md:max-w-md">
          <%= form.label :default_game_minutes, "기본 경기 시간", class: "label-text font-semibold mb-2" %>
          <div class="join w-full">
            <%= form.number_field :default_game_minutes,
                min: User::MIN_GAME_MINUTES,
                max: User::MAX_GAME_MINUTES,
                step: 1,
                class: "input input-bordered join-item w-full text-lg font-semibold",
                required: true %>
            <span class="join-item btn btn-disabled">분</span>
          </div>
          <div class="label">
            <span class="label-text-alt text-base-content/60">허용 범위: <%= User::MIN_GAME_MINUTES %>~<%= User::MAX_GAME_MINUTES %>분</span>
          </div>
          <% if @user.errors[:default_game_minutes].any? %>
            <p class="text-error text-sm mt-2"><%= @user.errors[:default_game_minutes].join(", ") %></p>
          <% end %>
        </div>

        <div class="flex items-center gap-3">
          <%= form.submit "저장하기", class: "btn btn-primary gap-2" %>
          <%= link_to "취소", clubs_path, class: "btn btn-ghost" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
