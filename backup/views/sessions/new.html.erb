<% content_for :title, "로그인 - 농구 클럽 매니저" %>

<div class="min-h-screen flex items-center justify-center p-4">
  <div class="card bg-base-100 shadow-xl w-full max-w-md">
    <div class="card-body">
      <!-- 로고 & 타이틀 -->
      <div class="text-center mb-6">
        <div class="text-6xl mb-4">🏀</div>
        <h1 class="text-3xl font-bold">로그인</h1>
        <p class="text-base-content/60 mt-2">클럽 관리로 바로 들어가세요</p>
      </div>

      <!-- 로그인 폼 -->
      <%= form_with url: session_path, method: :post, local: true, class: "space-y-4" do %>
        <div class="form-control w-full">
          <label class="label" for="email">
            <span class="label-text">이메일</span>
          </label>
          <%= email_field_tag :email, params[:email],
              required: true,
              autocomplete: "email",
              placeholder: "your@email.com",
              class: "input input-bordered w-full" %>
        </div>

        <div class="form-control w-full">
          <label class="label" for="password">
            <span class="label-text">비밀번호</span>
          </label>
          <%= password_field_tag :password, nil,
              required: true,
              autocomplete: "current-password",
              placeholder: "••••••••",
              class: "input input-bordered w-full" %>
        </div>

        <div class="form-control mt-6">
          <%= submit_tag "로그인", class: "btn btn-primary btn-lg w-full" %>
        </div>
      <% end %>

      <!-- 회원가입 링크 -->
      <div class="divider">OR</div>

      <div class="text-center">
        <span class="text-base-content/60">계정이 없으신가요?</span>
        <%= link_to "회원가입", new_registration_path, class: "link link-primary ml-1" %>
      </div>
    </div>
  </div>
</div>
