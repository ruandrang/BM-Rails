<% content_for :title, "내 클럽" %>

<div class="page-header">
  <div>
    <h1>내 클럽</h1>
    <p class="muted">클럽을 만들고 멤버, 팀, 경기를 관리하세요.</p>
  </div>
  <%= link_to "새 클럽 만들기", new_club_path, class: "button primary" %>
</div>

<div class="card-grid">
  <% @clubs.each do |club| %>
    <div class="card club-card is-clickable" data-href="<%= club_path(club) %>">
      <div class="club-icon"><%= club_icon(club.icon) %></div>
      <div class="club-info">
        <h2><%= link_to club.name, club_path(club) %></h2>
        <p class="muted">멤버 <%= club.members.count %>명 · 경기 <%= club.matches.count %>회</p>
      </div>
      <div class="card-actions">
        <%= link_to "열기", club_path(club), class: "button ghost" %>
        <%= link_to "편집", edit_club_path(club), class: "button ghost" %>
      </div>
    </div>
  <% end %>

  <% if @clubs.empty? %>
    <div class="empty-state">
      <p>아직 클럽이 없습니다. 첫 클럽을 만들어 보세요!</p>
    </div>
  <% end %>
</div>
