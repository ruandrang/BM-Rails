다음 순서로 배포 작업을 진행해줘:

1. `bin/rubocop`과 `bin/brakeman --no-pager`로 린트/보안 검사 실행
2. 검사 통과 시, 현재 변경사항을 확인하고 한국어 커밋 메시지로 dev 브랜치에 커밋
3. 새로운 DB 마이그레이션 파일이 있는지 확인하고, 있으면 알려줘
4. main 브랜치로 이동해서 dev 브랜치를 머지
5. origin main에 푸시 (Railway 자동 배포 트리거)
6. 다시 dev 브랜치로 돌아와
7. 각 단계에서 문제가 생기면 즉시 멈추고 알려줘
